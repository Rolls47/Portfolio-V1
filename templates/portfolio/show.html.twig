{% extends 'base.html.twig' %}

{% block title %}Portfolio{% endblock %}

{% block body %}
    <h1>Portfolio</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ portfolio.id }}</td>
        </tr>
        <tr>
            <th>Nom :</th>
            <td>{{ portfolio.name }}</td>
        </tr>
        <tr>
            <th>Description :</th>
            <td>{{ portfolio.description }}</td>
        </tr>
        <tr>
            <th>Technologie :</th>
            <td>{{ portfolio.technology }}</td>
        </tr>
        <tr>
            <th>Image :</th>
            <td><img class="logo-icon" src="{{ portfolio.image }}" alt="logo-skill"></td>
        </tr>
        <tr>
            <th>Lien :</th>
            <td>{{ portfolio.lien }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('portfolio_index') }}">Retour à la liste </a>

    <a href="{{ path('portfolio_edit', {'id': portfolio.id}) }}">Modifier</a>
    {{ include('portfolio/_delete_form.html.twig') }}
{% endblock %}
