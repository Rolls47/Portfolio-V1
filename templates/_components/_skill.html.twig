<div class="row mb-5">
    <div class="col-12 d-flex justify-content-center">
        <h1>Mes Compétences</h1>
    </div>
</div>
<div class="row justify-content-center align-content-center">
    {% for skill in skills %}
        <div class="col-3 text-center">
            <img src="{{ skill.image }}" alt="" class="image-skill">
        </div>
    {% endfor %}
</div>
