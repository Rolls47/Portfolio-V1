{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}


{% block title %}Modifier les Skill{% endblock %}

{% block body %}
    <h1>Modifier les Skill's</h1>

    {{ include('skill/_form.html.twig', {'button_label': 'Modifier'}) }}

    <div class="d-flex mt-5">
        {{ include('skill/_delete_form.html.twig') }}
        <a href="{{ path('skill_index') }}" class="ml-3">Retour à la liste</a>
    </div>
{% endblock %}
